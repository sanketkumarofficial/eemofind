<!doctype html><html><head><meta name="viewport" content="width=device-width,initial-scale=1"><title>Reset password</title>@vite(['resources/css/app.css'])</head><body><main class="auth-page"><div class="card auth-card p-5"><h3>Reset password</h3><p class="text-muted">We will email you a secure reset link.</p>@if(session('status'))<div class="alert alert-success">{{ session('status') }}</div>@endif<form method="POST" action="{{ route('password.email') }}">@csrf<input type="email" name="email" class="form-control mb-3" placeholder="Email address" required><button class="btn btn-primary w-100">Send reset link</button></form><a class="mt-3 text-center" href="{{ route('login') }}">Back to sign in</a></div></main></body></html>
