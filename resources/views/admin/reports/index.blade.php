@extends('layouts.app') @section('title','Reports') @section('content')<div class="d-flex justify-content-between align-items-center mb-4"><div><h2 class="fw-bold">Payments & report</h2><p class="text-muted mb-0">Revenue ledger and export center.</p></div><div class="dropdown"><button class="btn btn-primary dropdown-toggle" data-bs-toggle="dropdown">Export report</button><div class="dropdown-menu dropdown-menu-end p-2" style="min-width:240px">@foreach(['users','devices','groups','subscriptions','payments','tickets'] as $type)<div class="d-flex justify-content-between align-items-center px-2 py-1"><span>{{ str($type)->title() }}</span><span><a class="btn btn-sm btn-light" href="{{ route('admin.reports.export',[$type,'xlsx']) }}">XLSX</a><a class="btn btn-sm btn-light" href="{{ route('admin.reports.export',[$type,'pdf']) }}">PDF</a></span></div>@endforeach</div></div></div><div class="card"><div class="card-body"><form class="row g-2 mb-3"><div class="col-md-3"><input type="date" name="from" class="form-control" value="{{ request('from') }}"></div><div class="col-md-3"><input type="date" name="to" class="form-control" value="{{ request('to') }}"></div><div class="col-auto"><button class="btn btn-outline-primary">Apply</button></div></form><div class="table-responsive"><table class="table"><thead><tr><th>Order</th><th>User</th><th>Amount</th><th>Method</th><th>Status</th><th>Date</th></tr></thead><tbody>@forelse($payments as $payment)<tr><td>{{ $payment->order_id }}</td><td>{{ $payment->user?->name }}</td><td>₹{{ number_format($payment->amount,2) }}</td><td>{{ $payment->payment_method??'—' }}</td><td><span class="badge text-bg-{{ $payment->status==='success'?'success':($payment->status==='failed'?'danger':'warning') }}">{{ ucfirst($payment->status) }}</span></td><td>{{ $payment->created_at->format('d M Y') }}</td></tr>@empty<tr><td colspan="6"><div class="empty-state">No payment records.</div></td></tr>@endforelse</tbody></table></div>{{ $payments->links() }}</div></div>@endsection
