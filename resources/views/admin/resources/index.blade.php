@extends('layouts.app') @section('title',$config['title']) @section('content')<div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4"><div><h2 class="fw-bold mb-1">{{ $config['title'] }}</h2><p class="text-muted mb-0">Manage and monitor {{ strtolower($config['title']) }}.</p></div>@can($module.'.create')<a class="btn btn-primary" href="{{ route('admin.resources.create',$module) }}"><i class="bi bi-plus-lg me-1"></i>Add {{ $config['singular'] }}</a>@endcan</div><div class="card"><div class="card-body"><form class="row g-2 mb-3"><div class="col-md-5"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Search {{ strtolower($config['title']) }}..."></div><div class="col-auto"><button class="btn btn-outline-primary">Search</button></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr>@foreach($config['columns'] as $column)<th>{{ str($column)->replace('_',' ')->title() }}</th>@endforeach<th class="text-end">Actions</th></tr></thead><tbody>@forelse($records as $record)<tr>@foreach($config['columns'] as $column)<td>@php $value=$record->{$column}; if(str_ends_with($column,'_id') && $column==='owner_id')$value=$record->owner?->name; @endphp @if(is_bool($value))<span class="badge {{ $value?'text-bg-success':'text-bg-secondary' }}">{{ $value?'Yes':'No' }}</span>@elseif(in_array($column,['status']))<span class="badge badge-soft">{{ str($value)->replace('_',' ')->title() }}</span>@else{{ $value instanceof \Carbon\CarbonInterface?$value->diffForHumans():$value }}@endif</td>@endforeach<td class="text-end"><a class="btn btn-sm btn-light" href="{{ route('admin.resources.show',[$module,$record->id]) }}"><i class="bi bi-eye"></i></a>@can($module.'.update')<a class="btn btn-sm btn-light" href="{{ route('admin.resources.edit',[$module,$record->id]) }}"><i class="bi bi-pencil"></i></a>@endcan @can($module.'.delete')<form class="d-inline" method="POST" action="{{ route('admin.resources.destroy',[$module,$record->id]) }}" data-confirm="This cannot be undone.">@csrf @method('DELETE')<button class="btn btn-sm btn-light text-danger"><i class="bi bi-trash"></i></button></form>@endcan</td></tr>@empty<tr><td colspan="20"><div class="empty-state"><i class="bi bi-inbox fs-1 d-block mb-2"></i>No records found.</div></td></tr>@endforelse</tbody></table></div>{{ $records->links() }}</div></div>@endsection
