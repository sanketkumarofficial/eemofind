@extends('layouts.app') @section('title','Support Tickets') @section('content')<h2 class="fw-bold">Support tickets</h2><p class="text-muted">Triage, assign, reply, and resolve customer requests.</p><div class="card"><div class="card-body"><form class="row g-2 mb-3"><div class="col-md-4"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Ticket number or subject"></div><div class="col-md-2"><select class="form-select" name="status"><option value="">All statuses</option>@foreach(['open','in_progress','resolved','closed','reopened'] as $x)<option value="{{ $x }}" @selected(request('status')===$x)>{{ str($x)->replace('_',' ')->title() }}</option>@endforeach</select></div><div class="col-md-2"><select class="form-select" name="priority"><option value="">All priorities</option>@foreach(['low','medium','high','critical'] as $x)<option @selected(request('priority')===$x)>{{ $x }}</option>@endforeach</select></div><div class="col-auto"><button class="btn btn-primary">Filter</button></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Ticket</th><th>User</th><th>Subject</th><th>Priority</th><th>Status</th><th>Assigned</th><th></th></tr></thead><tbody>@forelse($tickets as $ticket)<tr><td><b>{{ $ticket->ticket_number }}</b><small class="d-block text-muted">{{ $ticket->created_at->diffForHumans() }}</small></td><td>{{ $ticket->user->name }}</td><td>{{ $ticket->subject }}</td><td><span class="badge text-bg-{{ $ticket->priority==='critical'?'danger':($ticket->priority==='high'?'warning':'secondary') }}">{{ ucfirst($ticket->priority) }}</span></td><td>{{ str($ticket->status)->replace('_',' ')->title() }}</td><td>{{ $ticket->assignee?->name??'Unassigned' }}</td><td><a class="btn btn-sm btn-light" href="{{ route('admin.tickets.show',$ticket) }}">Open</a></td></tr>@empty<tr><td colspan="7"><div class="empty-state">No tickets match your filters.</div></td></tr>@endforelse</tbody></table></div>{{ $tickets->links() }}</div></div>@endsection
